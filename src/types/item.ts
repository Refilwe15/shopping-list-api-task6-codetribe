export interface Item {
  id: number;          // Unique identifier for each shopping item
  name: string;        // Name of the item (e.g., "Milk")
  quantity: number;    // Quantity of the item (e.g., 2)
  price: number;       // Price per unit or total price (e.g., 30.5)
}
